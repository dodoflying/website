/**
 * Generated by "@vuepress/plugin-blog"
 */
export default {
  post: function (page, id, pid) {
                return page.pid === pid && page.id === id;
            },
  book: function (page, id, pid) {
                return page.pid === pid && page.id === id;
            },
}